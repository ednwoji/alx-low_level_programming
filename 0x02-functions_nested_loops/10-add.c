#include "main.h"


/**
 * int add(int a, int b) - Check description below
 * Description: For addition of two integers
 * Return: Result if Successful
 */
int add(int a, int b)
{
	int sum = a + b;
	return (sum);
}
