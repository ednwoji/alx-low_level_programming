#include "main.h"


/**
 * int mul(int a, int b) - Check description
 * Description: Multiplies two integers
 * @a: Input character
 * @b: input character
 * Return: Always 0 (Success)
 */
int mul(int a, int b)
{
	int product = a * b;
	
	return (product);
}
