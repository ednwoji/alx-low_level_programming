#include "main.h"

/**
 * _strlen - Returns the length of a string
 * @s: Input character
 * Return: Length of the string
 */
int _strlen(char *s)
{
	int length;
	
	length = strlen(*s);
	return (length);
}
